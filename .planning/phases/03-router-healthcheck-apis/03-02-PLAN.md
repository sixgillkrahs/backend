---
phase: 03-router-healthcheck-apis
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [internal/app/router.go, internal/app/handler/health.go]
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "D-10: /healthz returns a detailed JSON response showing the status of each dependent connection pool (PostgreSQL and Redis)"
    - "Dynamic database connection verification is performed on healthcheck call"
    - "Dynamic Redis cache connection verification is performed on healthcheck call"
  artifacts:
    - path: "internal/app/handler/health.go"
      provides: "HTTP handler for Postgres and Redis health verification"
      contains: "Healthcheck"
---

<objective>
Implement the /healthz endpoint containing active database and cache ping verifications to report system health.

Purpose: Provide diagnostic healthcheck endpoint for orchestration (e.g. Docker, Kubernetes) and verify wiring.
Output: Health check handler file and /healthz registered on router.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-router-healthcheck-apis/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Healthcheck Handler</name>
  <files>internal/app/handler/health.go</files>
  <action>Create internal/app/handler/health.go. Write HealthHandler(dbConn *gorm.DB, rdbClient *redis.Client) gin.HandlerFunc. In the handler: Ping PostgreSQL using dbConn.DB() and ping Redis using rdbClient.Ping(). If both are successful, return HTTP 200 with JSON status healthy and details. If either fails, log error and return HTTP 500 with detailed failure JSON.</action>
  <verify>Compile check health.go.</verify>
  <acceptance_criteria>
    - health.go contains "func HealthHandler"
    - health.go checks both database and redis connections
  </acceptance_criteria>
  <done>Healthcheck handler with active ping checks successfully implemented</done>
</task>

<task type="auto">
  <name>Task 2: Register healthz endpoint on router</name>
  <files>internal/app/router.go</files>
  <read_first>internal/app/router.go</read_first>
  <action>In internal/app/router.go, register GET /healthz using the newly created HealthHandler passing the db and redis instances.</action>
  <verify>Build/run application, test curl http://localhost:8080/healthz.</verify>
  <acceptance_criteria>
    - router.go imports "github.com/username/backend/internal/app/handler"
    - router.go registers "/healthz" GET route
  </acceptance_criteria>
  <done>Healthcheck route registered on Gin engine</done>
</task>

</tasks>

<verification>
- [ ] `curl http://localhost:8080/healthz` returns `{"status":"healthy","database":"up","cache":"up"}` with HTTP 200 status code when both Postgres and Redis are up
- [ ] Stopping Postgres container (e.g. `docker stop go_backend_postgres`) causes `/healthz` to return HTTP 500 stating database status is down
</verification>

<success_criteria>
- The dynamic healthcheck endpoint is active and accurately represents PostgreSQL and Redis connection pool availability.
</success_criteria>

<output>
After completion, create `.planning/phases/03-router-healthcheck-apis/03-02-SUMMARY.md`
</output>
