---
phase: 03-router-healthcheck-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/app/router.go, cmd/server/main.go]
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "D-08: Setup Gin router and route registration inside internal/app/router.go"
    - "D-09: Set the Gin running mode dynamically depending on environment configuration"
    - "D-11: /ping returns a simple JSON message 'pong'"
    - "D-12: Use Go's built-in slog structured logger to log incoming API requests"
  artifacts:
    - path: "internal/app/router.go"
      provides: "HTTP Gin engine router setup and middleware registration"
      contains: "SetupRouter"
---

<objective>
Configure the Gin HTTP router, set up request log middleware using slog, and define the /ping API route.

Purpose: Bootstrap the web server layer and verify routing is active.
Output: Router configuration file and main.go wired to listen on the configured port.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-router-healthcheck-apis/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement router setup package</name>
  <files>internal/app/router.go</files>
  <action>Create internal/app/router.go. Implement SetupRouter(cfg *config.Config, dbConn *gorm.DB, rdbClient *redis.Client) *gin.Engine. In this function, dynamically set gin.SetMode based on cfg.Env. Configure a Gin engine using gin.New(), add standard Recovery middleware, and register a custom slog request log middleware. Create a GET /ping route returning {"message": "pong"}.</action>
  <verify>Compile check router.go.</verify>
  <acceptance_criteria>
    - router.go contains "func SetupRouter"
    - router.go contains "gin.SetMode"
    - router.go contains "/ping" GET handler
  </acceptance_criteria>
  <done>router.go implemented with basic routing and dynamic mode settings</done>
</task>

<task type="auto">
  <name>Task 2: Wire Gin router and start HTTP server</name>
  <files>cmd/server/main.go</files>
  <read_first>cmd/server/main.go</read_first>
  <action>Update cmd/server/main.go to call app.SetupRouter passing config, db instance, and redis client. Start the server on cfg.Port using r.Run(":" + cfg.Port).</action>
  <verify>Run application and test curl http://localhost:8080/ping.</verify>
  <acceptance_criteria>
    - main.go calls "app.SetupRouter"
    - main.go calls "Run" or starts HTTP listener
  </acceptance_criteria>
  <done>HTTP router running and listening on port</done>
</task>

</tasks>

<verification>
- [ ] Application starts and binds successfully to port 8080
- [ ] `curl http://localhost:8080/ping` returns `{"message":"pong"}` and logs request detail in slog JSON format
</verification>

<success_criteria>
- Gin web server is running and responds to ping requests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-router-healthcheck-apis/03-01-SUMMARY.md`
</output>
