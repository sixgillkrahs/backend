---
phase: 02-directory-layout-connections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [go.mod, go.sum, cmd/server/main.go, internal/pkg/config/config.go, .env.example, .env]
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03]

must_haves:
  truths:
    - "D-03: Standard layout directories cmd/server, internal/app, internal/pkg exist"
    - "D-01: Configuration package is implemented under internal/pkg/config/config.go containing a Config struct"
    - "D-02: Use github.com/joho/godotenv to automatically load .env environment variables in local development mode"
    - "D-07: Use Go's built-in structured logger slog for logging events"
  artifacts:
    - path: "internal/pkg/config/config.go"
      provides: "Environment variables config parser using godotenv"
      contains: "LoadConfig"
    - path: "cmd/server/main.go"
      provides: "Application main entrypoint"
      contains: "main"
---

<objective>
Scaffold the standard Go layout directory structure, implement the configuration loader package, and create the main entry point with structured logging.

Purpose: Provide a solid and clean organization layout for the backend, with dynamic configuration and tracing capabilities.
Output: Go workspace directories structured, config manager written, and main.go booted.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-directory-layout-connections/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install configuration management dependencies</name>
  <files>go.mod, go.sum</files>
  <read_first>go.mod</read_first>
  <action>Download joho/godotenv and ensure all other core libraries (gin, gorm, go-redis) are fully tidied in go.mod.</action>
  <verify>Run go get github.com/joho/godotenv and go mod tidy.</verify>
  <acceptance_criteria>
    - go.mod contains "github.com/joho/godotenv"
    - go.mod contains "github.com/gin-gonic/gin"
  </acceptance_criteria>
  <done>Dependencies successfully updated and listed in go.mod</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Go layout directories</name>
  <files>cmd/server/.gitkeep, internal/app/.gitkeep, internal/pkg/.gitkeep</files>
  <action>Create target layout directories cmd/server, internal/app, internal/pkg/config, internal/pkg/db, internal/pkg/cache.</action>
  <verify>Check directory paths exist.</verify>
  <acceptance_criteria>
    - Directory cmd/server/ exists
    - Directory internal/app/ exists
    - Directory internal/pkg/config/ exists
  </acceptance_criteria>
  <done>Standard Go folders successfully structured</done>
</task>

<task type="auto">
  <name>Task 3: Implement Configuration Package</name>
  <files>internal/pkg/config/config.go</files>
  <action>Create internal/pkg/config/config.go. Define a Config struct wrapping APP_PORT, DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME, REDIS_HOST, REDIS_PORT, REDIS_PASSWORD, and a LoadConfig() function utilizing godotenv.Load() to read from .env.</action>
  <verify>Compile check the config package.</verify>
  <acceptance_criteria>
    - config.go contains "type Config struct"
    - config.go contains "func LoadConfig()"
  </acceptance_criteria>
  <done>Configuration package implemented and loading env variables</done>
</task>

<task type="auto">
  <name>Task 4: Copy and verify .env file</name>
  <files>.env, .env.example</files>
  <read_first>.env.example</read_first>
  <action>Create a local .env file using .env.example as the baseline. Add custom default values matching docker-compose settings.</action>
  <verify>Verify .env exists and is filled.</verify>
  <acceptance_criteria>
    - .env exists and contains DATABASE_URL or equivalent DB connection configs
  </acceptance_criteria>
  <done>.env created and configured for local dev</done>
</task>

<task type="auto">
  <name>Task 5: Implement cmd/server/main.go entrypoint</name>
  <files>cmd/server/main.go</files>
  <action>Create cmd/server/main.go. Load the configuration package and initialize Go's built-in slog logger. Print a startup message using slog.Info.</action>
  <verify>Build/run main.go successfully.</verify>
  <acceptance_criteria>
    - main.go contains "package main"
    - main.go contains "slog.Info"
  </acceptance_criteria>
  <done>main.go initialized with configuration loader and slog logger</done>
</task>

</tasks>

<verification>
- [ ] Directory cmd/server, internal/app, internal/pkg exist on filesystem
- [ ] `go run cmd/server/main.go` runs without panic and logs configuration success
- [ ] go.mod is tidied and contains all core packages
</verification>

<success_criteria>
- The project structure is fully bootstrapped, configuration values load dynamically from env, and standard logging is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/02-directory-layout-connections/02-01-SUMMARY.md`
</output>
