---
phase: 02-directory-layout-connections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [internal/pkg/db/postgres.go, migrations/000001_init_schema.up.sql, migrations/000001_init_schema.down.sql, cmd/server/main.go]
autonomous: true
requirements: [DB-01, DB-02, DB-03]

must_haves:
  truths:
    - "D-05: Database connection pool is initialized using Gorm with default Postgres driver"
    - "D-04: Place PostgreSQL connection under internal/pkg/db/postgres.go"
    - "D-06: Database migrations will be structured SQL files located under ./migrations/ directory"
    - "Database migration runner is integrated and executed on startup"
  artifacts:
    - path: "internal/pkg/db/postgres.go"
      provides: "GORM DB connection pool and auto-migration runner using golang-migrate"
      contains: "InitPostgres"
    - path: "migrations/000001_init_schema.up.sql"
      provides: "Initial PostgreSQL schema setup SQL script"
      contains: "CREATE TABLE"
---

<objective>
Configure the PostgreSQL connection pool using GORM and set up the golang-migrate library to execute migration SQL files automatically on application boot.

Purpose: Provide persistence and ensure schema migrations are always applied before application processes serve requests.
Output: Postgres DB pool initialiser, migrations directory with schema setup files, and migration bootstrapper.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-directory-layout-connections/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install GORM Postgres driver and golang-migrate libraries</name>
  <files>go.mod, go.sum</files>
  <read_first>go.mod</read_first>
  <action>Download gorm.io/driver/postgres, and github.com/golang-migrate/migrate/v4 including its postgres driver.</action>
  <verify>Run go get gorm.io/driver/postgres and go get github.com/golang-migrate/migrate/v4 and its components, then tidy.</verify>
  <acceptance_criteria>
    - go.mod contains "gorm.io/driver/postgres"
    - go.mod contains "github.com/golang-migrate/migrate/v4"
  </acceptance_criteria>
  <done>Database and migration driver packages resolved</done>
</task>

<task type="auto">
  <name>Task 2: Implement Postgres connection pool</name>
  <files>internal/pkg/db/postgres.go</files>
  <action>Create internal/pkg/db/postgres.go. Write InitPostgres(cfg *config.Config) (*gorm.DB, error) to establish database connection, configure SQL DB settings (MaxOpenConns, MaxIdleConns, ConnMaxLifetime) via sql.DB, and ping the database to ensure connection is live.</action>
  <verify>Compile check postgres.go.</verify>
  <acceptance_criteria>
    - postgres.go contains "func InitPostgres"
    - postgres.go sets database pool parameters (e.g. SetMaxOpenConns)
  </acceptance_criteria>
  <done>PostgreSQL client wrapper with connection pooling successfully implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create initial migration files</name>
  <files>migrations/000001_init_schema.up.sql, migrations/000001_init_schema.down.sql</files>
  <action>Create migrations directory. Write 000001_init_schema.up.sql creating a dummy "users" table (id, name, created_at) to serve as a proof-of-concept schema. Write 000001_init_schema.down.sql dropping that table.</action>
  <verify>Check SQL files exist.</verify>
  <acceptance_criteria>
    - 000001_init_schema.up.sql contains "CREATE TABLE users"
    - 000001_init_schema.down.sql contains "DROP TABLE IF EXISTS users"
  </acceptance_criteria>
  <done>First SQL schema migration files created</done>
</task>

<task type="auto">
  <name>Task 4: Integrate migration runner in postgres init</name>
  <files>internal/pkg/db/postgres.go</files>
  <read_first>internal/pkg/db/postgres.go</read_first>
  <action>In internal/pkg/db/postgres.go, write a helper function RunMigrations(dbURL string) error using github.com/golang-migrate/migrate/v4 to run migrations from the file://migrations folder against the postgres DB. Invoke it inside InitPostgres or main.go boot sequence.</action>
  <verify>Compile and run schema migrations check.</verify>
  <acceptance_criteria>
    - postgres.go imports "github.com/golang-migrate/migrate/v4"
    - postgres.go contains logic to run migrations from "file://migrations"
  </acceptance_criteria>
  <done>golang-migrate integration successfully added to application startup</done>
</task>

<task type="auto">
  <name>Task 5: Wire Postgres initialization into main.go</name>
  <files>cmd/server/main.go</files>
  <read_first>cmd/server/main.go</read_first>
  <action>Update cmd/server/main.go to load db.InitPostgres using configuration from internal/pkg/config package. Handle connection failure gracefully, logging with slog.</action>
  <verify>Run the application (with docker-compose postgres running) and verify it successfully connects and runs migrations.</verify>
  <acceptance_criteria>
    - main.go calls "db.InitPostgres"
    - Startup logs show migration success or no new migrations to run
  </acceptance_criteria>
  <done>PostgreSQL and migrations wired into main entrypoint</done>
</task>

</tasks>

<verification>
- [ ] migrations/000001_init_schema.up.sql exists
- [ ] Application starts, connects to PostgreSQL container, runs migrations, and successfully creates the dummy 'users' table
- [ ] Verifying database tables list contains 'users' and 'schema_migrations' after boot
</verification>

<success_criteria>
- PostgreSQL GORM connection is established and SQL-based migrations are successfully applied on startup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-directory-layout-connections/02-02-SUMMARY.md`
</output>
