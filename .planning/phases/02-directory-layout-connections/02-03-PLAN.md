---
phase: 02-directory-layout-connections
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [internal/pkg/cache/redis.go, cmd/server/main.go]
autonomous: true
requirements: [CACHE-01]

must_haves:
  truths:
    - "D-04: Place Redis connection under internal/pkg/cache/redis.go"
    - "Redis client is initialized and connected using go-redis/v9"
    - "Redis ping test passes successfully during application boot"
  artifacts:
    - path: "internal/pkg/cache/redis.go"
      provides: "Redis client initialization and connection wrapper"
      contains: "InitRedis"
---

<objective>
Configure the Redis client connection pool using go-redis/v9 and integrate it into the application startup boot sequence.

Purpose: Provide caching capabilities and verify that the application can successfully connect to Redis on boot.
Output: Redis client package and ping verify on startup.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-directory-layout-connections/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install go-redis/v9 library package</name>
  <files>go.mod, go.sum</files>
  <read_first>go.mod</read_first>
  <action>Download github.com/redis/go-redis/v9 using go get.</action>
  <verify>Run go get github.com/redis/go-redis/v9 and tidy.</verify>
  <acceptance_criteria>
    - go.mod contains "github.com/redis/go-redis/v9"
  </acceptance_criteria>
  <done>go-redis dependency package resolved</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redis connection wrapper</name>
  <files>internal/pkg/cache/redis.go</files>
  <action>Create internal/pkg/cache/redis.go. Write InitRedis(cfg *config.Config) (*redis.Client, error) establishing the Redis client, and run Ping(ctx) to verify the connection is active.</action>
  <verify>Compile check redis.go.</verify>
  <acceptance_criteria>
    - redis.go contains "func InitRedis"
    - redis.go calls "redis.NewClient"
  </acceptance_criteria>
  <done>Redis client initialization module successfully implemented</done>
</task>

<task type="auto">
  <name>Task 3: Wire Redis connection into main.go</name>
  <files>cmd/server/main.go</files>
  <read_first>cmd/server/main.go</read_first>
  <action>Update cmd/server/main.go to call cache.InitRedis after configuration is loaded. If it fails, log error with slog and terminate or handle accordingly.</action>
  <verify>Run application (with docker-compose Redis running) and verify it logs successful connection to Redis.</verify>
  <acceptance_criteria>
    - main.go calls "cache.InitRedis"
    - Startup logs show successful Redis connection ping
  </acceptance_criteria>
  <done>Redis connection verified and wired into main bootstrapper</done>
</task>

</tasks>

<verification>
- [ ] internal/pkg/cache/redis.go exists
- [ ] Application starts, connects to Redis container, pings it, and outputs success log without error
</verification>

<success_criteria>
- Redis client is configured with connection pooling and verified active on application startup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-directory-layout-connections/02-03-SUMMARY.md`
</output>
