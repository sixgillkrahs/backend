---
phase: 01-environment-tooling
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [docker-compose.yml, .air.toml, .env.example]
autonomous: true
requirements: [ENV-05, ENV-06]

must_haves:
  truths:
    - "PostgreSQL and Redis services can be run locally using Docker Compose"
    - "Air configuration file exists and defines build/run watch patterns"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local services (Postgres, Redis) orchestration"
      contains: "image: postgres:16-alpine"
    - path: ".air.toml"
      provides: "Hot-reload configuration for local development"
      contains: "cmd ="
    - path: ".env.example"
      provides: "Environment variables configuration template"
      contains: "DB_PORT"
---

<objective>
Configure local service orchestration via Docker Compose and live reloading via Air.

Purpose: Automate launching Postgres/Redis databases and set up hot reloading for Go code changes.
Output: docker-compose.yml, .air.toml, and .env.example.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-tooling/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>Create docker-compose.yml configuring postgres (postgres:16-alpine, port 5432, user/db/password=postgres, volume mount for persistence) and redis (redis:7-alpine, port 6379, volume mount).</action>
  <verify>Validate docker-compose configuration using docker-compose config.</verify>
  <acceptance_criteria>
    - docker-compose.yml contains service "postgres"
    - docker-compose.yml contains service "redis"
    - docker-compose.yml references postgres:16-alpine and redis:7-alpine
  </acceptance_criteria>
  <done>docker-compose.yml is configured with Postgres and Redis services</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example</name>
  <files>.env.example</files>
  <action>Create .env.example mapping the default database connections (DB_HOST=localhost, DB_PORT=5432, DB_USER=postgres, DB_PASSWORD=postgres, DB_NAME=postgres) and Redis connection (REDIS_HOST=localhost, REDIS_PORT=6379, REDIS_PASSWORD=).</action>
  <verify>Check .env.example contents.</verify>
  <acceptance_criteria>
    - .env.example contains DB_PORT=5432
    - .env.example contains REDIS_PORT=6379
  </acceptance_criteria>
  <done>.env.example config template is created</done>
</task>

<task type="auto">
  <name>Task 3: Create .air.toml</name>
  <files>.air.toml</files>
  <action>Create a default .air.toml config file. It should specify building the binary from cmd/server/main.go, outputting to tmp/main, and watching files with go extension, excluding tmp/ and vendor/ directories.</action>
  <verify>Check that .air.toml exists and contains build command mapping.</verify>
  <acceptance_criteria>
    - .air.toml contains "cmd/server" or "tmp/main"
  </acceptance_criteria>
  <done>.air.toml configuration exists</done>
</task>

</tasks>

<verification>
- [ ] docker-compose.yml passes config check
- [ ] .air.toml exists and is valid
- [ ] .env.example contains database connection variables
</verification>

<success_criteria>
- Developers can start the environment using docker-compose up -d and run the app with live reloading via Air.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-tooling/01-02-SUMMARY.md`
</output>
